#include <stdio.h>
#include <stdlib.h>

int main(void){
    int test, n, m, a, b;
    scanf("%d", &test);

    for (int i = 0; i < test; i++){
        scanf("%d %d", &n, &m);
        int* arr
        for (int j = 0; j < m; j++){
            scanf("%d %d", &a, &b);
        }
    }

    return 0;
}